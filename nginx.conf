events {}

http {
    upstream backend {
        server analysis-0:8000;
        server analysis-1:8000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;
        }
    }
}